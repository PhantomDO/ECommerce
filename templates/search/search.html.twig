{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        {{ form_row(form.title) }}
        {{ form_row(form.price) }}
        {{ form_choicetype(form.city) }}

    <button type="submit">Search</button>
    {{ form_end(form) }}
{% endblock %}